<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.digitalchina.sport.mgr.resource.dao.StadiumDao" >
    <select id="findProvienceList" resultType="java.util.Map">
        SELECT
	      id,
	      name
        FROM
	      province_city_coutry
        WHERE
	      id = '320000'
    </select>
</mapper>